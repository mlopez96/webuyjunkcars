import { lazy } from "react";
const Container = lazy(() => import("../../common/Container"));

const FAQ = () => {
return (
  <Container>
    <h1>Frequently Asked Questions</h1>

  </Container>
  );
};

export default FAQ;
